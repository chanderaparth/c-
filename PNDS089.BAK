/* Finding Factorial with while loop */

#include <stdio.h>
#include <conio.h>

int main() {
  int i=1, n;
  long int f=1;

  clrscr();

  printf("Enter end no: ");
  scanf("%d", &n);

  while(i<=n) {
    f *= i;
    printf("%d = %ld\n", i, f);
    i++;
  }

  getch();
  return 0;
}